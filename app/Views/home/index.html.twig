{% extends 'base.html.twig' %}

{% block body %}
    <h1>{{ controllerName }}</h1>

    <ul>
        {% for item in items %}
            <li>{{ item.name }}</li>
        {% endfor %}
    </ul>
{% endblock %}
